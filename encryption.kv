#:import utils kivy.utils
# used for hex color... ex:
# utils.get_color_from_hex("#FFFFFF")

<UnicodeEncryption>
    BoxLayout:
        orientation: "horizontal"
        width: root.width
        height: root.height
        TextInput:
            id: input
        BoxLayout:
            size_hint_x: .25
            orientation: "vertical"
            Button:
                text: "encrypt from text"
                on_release: app.encrypt_text()
            Button:
                text: "aaa"
                on_release: app.encrypt_file()
